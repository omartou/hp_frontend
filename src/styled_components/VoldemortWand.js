import styled from "styled-components";
import voldemort from "../images/voldemort2.png";

const VoldemortWandImage = styled.img`
  content: url(${voldemort});
  filter: sepia(100%);
  margin: auto;
`;

export default VoldemortWandImage;
