import styled from "styled-components";
import draco from "../images/draco2.png";

const DracoWandImage = styled.img`
  content: url(${draco});
  filter: sepia(100%);
  margin: auto;
`;

export default DracoWandImage;
