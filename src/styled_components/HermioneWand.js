import styled from "styled-components";
import hermione from "../images/hermione2.png";

const HermioneWandImage = styled.img`
  content: url(${hermione});
  filter: sepia(100%);
  margin: auto;
`;

export default HermioneWandImage;
